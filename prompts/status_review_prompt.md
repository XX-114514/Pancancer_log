# 状态复核提示

请只读核对 `Project_v3` 最新权威证据，并判断 `STATUS.md` 是否仍准确。

要求：

1. 先读取本仓库 `AGENTS.md`、`STATUS.md`、`ROADMAP.md`、`TODO.md` 和最近相关日志。
2. 检查最新 run index、主 run record、关键 validation summary。
3. 将每个状态结论标为已观察事实、解释、假设或建议。
4. 不因目录或文件存在就推断运行成功。
5. 仅在当前真实状态发生变化时修改 `STATUS.md`。
6. 创建 Codex task log，记录文件、命令、证据、未验证项和建议 commit。
7. 运行仓库验证并检查完整 diff。
