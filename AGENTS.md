# AGENTS.md

本文件定义 Codex 在本记录仓库中的长期工作规则。上级工作区的规则继续生效；如有冲突，采用更严格且不破坏历史的规则。

## 1. 每次工作前必须读取

开始任何有意义的任务前：

1. 阅读 `README.md`、`STATUS.md`、`ROADMAP.md`、`TODO.md`。
2. 阅读与任务相关的 `methods/` 和 `docs/`。
3. 阅读最近且相关的 daily log 与 Codex task log。
4. 阅读相关的 run、incident 和 decision 记录。
5. 执行 `git status --short`，识别并保留已有未提交修改。
6. 若需从外部分析工作区取证，只做任务需要的最小范围检查，不复制大型文件。

## 2. Codex 任务日志是强制记录

每次有意义的任务必须建立：

```text
logs/codex/YYYY/YYYY-MM-DD_task-slug.md
```

日志至少记录：

- 用户要求；
- 开始前的项目状态；
- 初始计划；
- 检查过的文件；
- 执行过的命令；
- 关键发现；
- 修改过的文件及原因；
- 验证方式和结果；
- 失败尝试；
- 未验证内容；
- 尚未解决的问题；
- 建议的下一步；
- 建议的 Git commit message。

同一任务优先持续更新同一日志，不因重试制造多个含义重复的文件。

## 3. 严格区分信息性质

记录时明确区分：

- **已观察事实**：有可直接检查的文件、命令输出或运行记录支持。
- **解释**：基于事实的合理含义，应说明依据。
- **假设**：尚未验证，必须显式标记。
- **建议**：未来动作或方案，不代表已经执行。

不得把推测写成事实。没有可检查证据时，不得声称流程、运行、分析或验证成功。旧记录与新证据冲突时，保留旧背景并记录纠正依据。

## 4. 文档各司其职

- `STATUS.md`：只维护当前真实状态，不写时间流水账。
- `ROADMAP.md`：维护阶段、里程碑、依赖和总体规划。
- `TODO.md`：维护可执行任务、优先级、依赖和完成标准。
- `CHANGELOG.md`：记录重要项目变化，不记录每个小改动。
- `logs/daily/`：记录每天发生了什么。
- `logs/codex/`：记录 Codex 每次具体检查、创建、修改和验证了什么。
- `logs/incidents/`：记录有影响的故障及修复证据。
- `runs/`：记录正式运行的输入、参数、命令、输出和结果。
- `methods/`：记录当前认可的方法与分析逻辑。
- `decisions/`：记录重要方案选择和理由。
- `inventories/`：维护数据集、运行和产物的结构化清单。

避免复制同一段内容。先确定权威文件，再用相对链接引用。

## 5. Git 与文件安全

修改前执行：

```bash
git status --short
```

修改后执行：

```bash
git status --short
git diff --stat
git diff
bash scripts/validate_repository.sh
```

禁止执行：

```bash
git reset --hard
git clean -fd
git checkout -- .
git restore .
git rebase
git commit --amend
git push --force
git push --force-with-lease
```

也禁止：

- 删除已有文件或目录；
- 为“整洁”而移除失败历史；
- 大规模重命名；
- 覆盖无关的未提交修改；
- 修改 Git 历史；
- 将未知远程假定为 private；
- 在远程未配置或目标不明确时推送。

用户已于 2026-07-26 明确允许 Codex 对本独立记录仓库执行常规 `git commit` 和向已确认 private 远程执行普通 `git push`。该授权不包括强制推送、删除、历史改写，也不扩展到上级分析工作区或嵌套代码仓库。每次提交前仍必须检查 diff 和验证结果。

## 6. 隐私与安全

不得写入或提交：

- 密码、API key、access token、SSH 私钥、Cookie、数据库凭据；
- 患者身份信息、受控访问数据、原始人类测序数据；
- 未经审查的样本级敏感元数据；
- 大型分析矩阵、中间对象、完整环境导出或完整作业日志；
- 无必要的真实用户名、内部服务器地址和绝对路径。

路径优先写为：

```text
${PROJECT_ROOT}
${DATA_ROOT}
${RESULTS_ROOT}
${RUN_ROOT}
```

命令或日志包含敏感参数时，只记录经过脱敏的结构，不记录秘密值。准备 push 前必须运行验证脚本并人工查看 diff。

## 7. 运行记录要求

正式运行应记录：

- Conda 环境；
- Python/R 版本；
- 代码仓库、branch 和 commit；
- 输入与输出标识；
- 参数和完整可复现命令；
- 开始/结束时间与时区；
- 主机或调度器作业标识（公开前审查）；
- 退出状态；
- 输出验证方式和结果。

记录完整命令不等于复制完整 stdout/stderr；大型日志留在 `${RUN_ROOT}`，这里只保存逻辑路径、摘要和必要错误片段。

## 8. 历史记录与纠正

不要为了让项目看起来整洁而重写失败历史。发现旧记录错误时：

1. 保留原始历史背景；
2. 在新日志中说明纠正；
3. 给出可检查依据；
4. 更新 `STATUS.md` 等当前权威状态；
5. 必要时建立 incident 或 ADR；
6. 使用新的提交记录纠正，不修改旧提交。
