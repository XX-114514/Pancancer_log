# 故障记录

本目录记录对数据完整性、结果可信度、运行进度或复现有实质影响的故障。普通命令拼写错误留在 daily/Codex log，不必升级为 incident。

命名格式：

```text
logs/incidents/YYYY/YYYY-MM-DD_short-title.md
```

使用 `templates/incident_template.md`。根因没有证据时必须标记“待确认”；修复后需要独立验证，且不得删除原失败记录。
